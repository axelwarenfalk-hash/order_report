[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "order-report"
version = "0.1.0"
description = "Generate reports from order data."
requires-python = ">=3.10"
dependencies = [
    "pandas",
]

[tool.setuptools.packages.find]
where = ["src"]
